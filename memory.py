# Placeholder for vector database logic
def store_embedding(text, embedding):
    print(f"Storing: {text} -> {embedding[:5]}...")

def retrieve_similar(text_embedding):
    print(f"Retrieving similar for: {text_embedding[:5]}...")
    return []
